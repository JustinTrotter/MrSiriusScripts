using UnityEngine;
using System.Collections;

public class actionTriggerScript : MonoBehaviour {

	public enum TriggerName {brushTeeth, takeShower, eatFood, getDressed, goToWork, getUmbrella, getKeys};
	public TriggerName triggerName;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
